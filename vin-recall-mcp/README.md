# vin-recall-mcp (server package)

See the [repo-root README](../README.md) for what this server does, architecture, tool contracts, install instructions, caveats, and how to run the eval suite.

This directory is the Node/Express project itself (`package.json`, `src/`, `evals/`).
